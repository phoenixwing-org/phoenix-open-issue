# Phoenix Admin 插件迁移审计

状态：template active / Issue interim

详细审计底稿和多插件纵向统计后续放到 `phoenix-ai-workspaces`；本文件只保留源码仓库需要共同遵守的统计口径、当前 Issue 快照和验收结论。

## 1. 统一报告模板

每个迁移项目必须按相同顺序报告，并把数字标为“实测”“估算”或“待测”。

### A. 身份与基线

- 项目、插件 `moduleId`、版本；
- 不可变 legacy commit；
- Host、Wing、COOL 兼容版本；
- 统计日期和复算命令。

### B. UI 保真

- legacy 与插件的路由 View 数、业务组件数和 UI 源码行数；
- template/style 完全一致数、允许差异数、未经批准差异数；
- 浏览器截图和关键交互回归结果；
- 未迁移页面及其归属理由（插件、Host、淘汰）。

### C. 算法与单元测试

- 迁移的纯算法文件/行数；
- 继承、增加、删除的测试文件和测试用例数；
- 算法是否仍依赖 Vue、Host、网络或数据库；
- 类型检查、单测、契约测试和构建结果。

### D. 框架复用

逐项列出身份、用户、部门、字典、文件、路由、菜单、权限、审计、任务、备份和 Wing 区域贡献：

1. legacy 自建实现；
2. COOL/Wing/Pah 替代能力；
3. 插件保留的 adapter；
4. 被消除的文件/行数；
5. 仍需插件拥有的领域规则；
6. 验证证据。

### E. 规模与交付物

必须同时报告，不能把其中一种体积冒充另一种：

- 源码文件数、物理行、非空行、UTF-8 字节；
- 插件压缩包大小；
- 安装后磁盘大小；
- 前端增量 chunk 原始/gzip/brotli；
- 后端产物与 migration 大小；
- peer dependency 与实际被重复打包的 dependency；
- 新增 Host adapter 成本与未迁移平台壳收益。

### F. 复杂度趋势

- 本插件从零搭建耗时；
- 使用共享脚手架后新增一个 View、API、Entity 的改动文件数；
- 相比前一个插件，专用 adapter 行数和重复配置是否下降；
- 每千行业务代码的测试用例数；
- 下一插件可直接复用的脚本、Fixture、Action 或 npm 包。

### G. 结论

- `pass`：UI/算法/关键交互无未解释回退，接口闭环，数字可复算；
- `conditional pass`：迁移切片已验证，但后端或交付包仍有明确待办；
- `fail`：重做 UI、丢算法、复用造成领域语义损失，或统计不可复算。

## 2. Issue 当前审计快照

结论：`conditional pass`。列表、Issue、点检、Function、推送、8D 和仪表盘推送待办的 Midway 纵切已形成；数据库修正与固定测试工具已收敛为一个维护 View。权限、字典、repair ledger 和可信备份 manifest adapter 的源码门禁已通过，但整改 manifest 尚未装配到当前运行时，真实字典物化、非 root 权限矩阵、备份恢复和新 View 浏览器旅程仍禁止生产切换。

旧站业务导入纵切已在 2026-08-04 本地 Hub/PostgreSQL 完成真实执行与重复执行验收：首轮核心写入 37 行、跳过重复 1 行、8D 写入 3 行；第二轮核心与 8D 均写入 0 行，分别跳过已存在 38 行和 3 行。该证据关闭一次性导入的判重/幂等门禁，但不替代正式环境可信备份、恢复演练及其余插件发布门禁。

复算命令：

```bash
pnpm admin-plugin:audit
pnpm admin-plugin:verify
```

当前实测：

| 范围 | 文件数 | 物理行 | 非空行 | 源码字节 |
| --- | ---: | ---: | ---: | ---: |
| Legacy 业务依赖闭包 | 76 | 10,040 | 9,056 | 357,865 |
| 插件业务依赖闭包 | 78 | 9,320 | 8,429 | 337,672 |
| UI View/组件 | 32 | 6,719 | 6,185 | 244,773 |
| 继承的 legacy 算法 | 5 | 318 | 279 | 10,407 |
| 插件当前纯领域/算法 | 14 | 1,223 | 1,094 | 37,612 |
| 继承的算法单元测试 | 3 | 373 | 307 | 13,339 |
| 插件当前全部单元测试 | 17 | 1,498 | 1,328 | 51,698 |
| 明确未迁移的平台壳 | 7 | 870 | 801 | 29,179 |
| 新增 Host adapter | 6 | 188 | 160 | 5,655 |
| 生产 DDL migration | 2 | 217 | 199 | 7,782 |
| 当前插件源码包 | 140 | 16,192 | 14,629 | 561,501 |

补充证据：

- 31 个纳入追踪的 Vue 文件中，3 个 template/style 与 `legacy/2cdc5ea` 严格一致；28 个声明差异覆盖用户批准的 BOM 紧凑 Editor、最新 Wing 页面布局薄适配、8 个 PrimaryPanel/Section、8D/功能表动作去重、维护/测试合页、Host 语义 token、Function 停用生命周期和 Cool 用户列表权限显示；未经声明差异为 0；
- Admin 运行时已消费 Wing `eb131a4`：Ribbon 未选中项 computed color 为 `#64748b`，当前项为 `#1d4ed8`，图标继承按钮颜色且所有 `pnw:*` 均无 fallback；线框/实心图标的视觉重量另行验收；
- 5 个 legacy 算法文件原样纳入；加上 Midway 的纯 `domain/*.ts` 后当前共有 14 个纯领域/算法文件。3 个继承算法测试文件共 55 项；加入 Midway 领域、字典 catalog/契约、修正规划、ledger、Host capability、权限、Host-owned 设置边界、共享 View 布局和 `OpenIssueAccessService` 服务级资源权限测试后，插件门禁实测 17 文件、97/97 项通过；其中新增 6 项直接覆盖匿名 401、root/404、viewer/reporter/editor 和跨列表只读不提权；
- 独立 App、main、Router、AppShell、Login 和 OAuth 共 7 个平台壳文件未进入插件；
- Host-owned 设置 verifier 扫描 108 个生产源文件：manifest 必须声明 `identity/users/departments/roles`，不得物化账号/登录/密码/组织 route 或 endpoint，不得携带 legacy `systemRole` 和登录/组织类型；Host 用户列表必须消费 `base:sys:user:list`，`base_sys_user` 后端 adapter 只读，daily maintenance 只允许 `checkpoints+links`；3 项 fail-closed 回归通过；
- Issue dictionary contract verifier 与 3 项突变回归要求 catalog/消费 key 精确闭合、type key 不超过 128、preset/customizable 无非法或重复声明、core 协议值不受 preset 过滤且顺序不可定制；Pah 又补充目标 type 下未知自定义 value 重复的 dry-run 拒绝，避免只检查产品默认项而遗漏数据库唯一性冲突；
- 隔离 Host Vue `1a0d5ae` 复核发现 128 字符常量和 validator 曾误挂到“名称”字段、真正 Key 仍为 20；已修正到真实 `prop: 'key'`，新增字段归属回归，并通过 Host Vue 17 files / 56 tests、type-check 与 production build。该证据证明长 key UI 源码闭合，但不等于主 Host 已合并部署；
- 6 个 Host adapter 是当前复用成本，不能从“省掉的代码”中隐去；
- 当前业务依赖闭包比 legacy 少 720 个物理行；后端新增代码和测试已另计入插件源码包，不能把净减少冒充完整收益；
- manifest v2 当前声明 2 个 SQL，共 7,782 bytes；`pah-plugin.artifacts.json`、migration ID/path 与原始字节 SHA-256 由插件门禁一一对应。备份范围门禁另从 SQL `CREATE TABLE`、TypeORM Entity 和 manifest `dataOwnership` 交叉验证，当前 9 张 `oip_*` 表三方闭合，3 项契约回归通过。既有隔离事务与双库恢复证据只覆盖 v1 的 8 张表和黄金记录；新增 `0002-repair-ledger.sql` 尚未执行，不得沿用 v1 证据宣称 9 张表已完成恢复闭环；
- Pah 通用 DDL 契约由 Admin Node `e2d45998` 提供。隔离 production build 实测 `cool entity`、bundle、`mwtsc` 和通用制品复制全部通过；dist descriptor 为 91 bytes、SQL 为 6,943 bytes，源/manifest/dist SHA 均为 `sha256:e6612af156e3a7f06c0cf3a094416967c79e42383284acb0e60000574a6b8676`；临时克隆已删除，Host tracked tree 产品路径为 0；
- Admin Vue `1a0d5ae` 收口真实 404、参数型 hidden route 父模块归属、manifest v2 轻校验与 DDL 普通安装双重门禁；16 个 Pah 测试文件、52 项测试、全量类型检查和 production build 通过。浏览器冷启动 `/open-issue/issue/:id` 后业务 Ribbon、Primary Block、Issue 正文和 Footer 父链同时存在；真实未知地址稳定停在 `/404`，不循环；
- 受控本机挂载后，`/pah/plugins` 已登记 1 个 SQL migration。真实 dry-run 显示制品已校验、事务必须、备份需可信证明、到期时间及 `phoenix-open-issue-bootstrap v1` 待应用；页面没有 DDL 执行入口，普通“安装”不会向含 migration 的插件显示；
- `verify-admin-plugin-pack.mjs` 调用真实 `npm pack --dry-run` 实测当前源码交付包 145,048 bytes、121 项，bundled dependencies 为 0；无调用者的 legacy external-auth/org-tree 类型已从交付闭包删除；允许根目录、前后端 entrypoint、2 个 migration 制品和 license 元数据全部校验，测试、fixture、Vitest/tsconfig 和 `node_modules` 进包数为 0；
- 本轮 UI dirty 版本已在 `/tmp` 隔离的 Admin Vue `a26007fc` 源码副本上，以本地 Wing `0.6.2@e21f4727` 完成 production build：2,869 modules；dashboard、8D、functions、maintenance 四个 chunk 分别为 15,579 / 6,385 / 12,962 / 15,422 bytes，未生成 test-runner chunk，也未出现 `types/Pnw*` empty chunk。临时 Host 和 dist 已删除；这证明生产编译闭包，不替代真实菜单物化与浏览器交互。
- 第二轮列表/详情/推送收口在 `/private/tmp` 隔离 Host Vue `03c0c4b5` + Wing `0.6.2@e21f4727` 再次完成 production build：2,869 modules；lists、list-detail、push-history、issue-detail JS raw 分别为 11,443 / 46,418 / 8,836 / 40,700 bytes，gzip 为 4,478 / 14,561 / 3,776 / 13,108 bytes。日志只有 4 个 Host 通用依赖 empty chunk，没有旧 `types/Pnw*`；临时目录已删除。
- 先前四页共享 `PnwPageLayout` 薄层在隔离 Host Vue `03c0c4b5` + 精确 Wing `0.6.2@e21f4727` 完成 production build：2,872 modules；无独立 test-runner chunk、无 `types/Pnw*` empty chunk。该制品对应旧 16px 消费侧 gutter，现只作编译回归历史。
- 同一旧版本曾在只读临时浏览器 fixture 完成布局关卡 A：1280×720 / 600×800、明/暗主题下 Dashboard、8D、功能表、维护与 test-runner 同 View 深链均为左右 16px/上下 0，Primary 开合和窄屏堆叠的 toggle/h1 overlap 均为 false；console warn/error 为 0。Wing current 已用默认 MainBlock 10px 取代该尺寸，因此该 fixture 不关闭当前布局关卡。
- 当前七个普通数据 View 已统一消费 `PoiCompactEditorView` / Wing 默认 MainBlock 10px；Issue 详情作为完整卡片保留四向 16px。8 个真实 Primary contribution 已统一消费 Wing `PnwPrimaryPanel/PnwPrimarySection`，Section 状态按稳定 View/实体 key 隔离并限制为 64 组。布局、manifest、settings boundary、closure、UI fidelity、Vue typecheck 和 pack 门禁全通过，插件总计 17 files / 101 tests，UI 为 3 exact + 28 approved deltas，pack 为 121 项 / 145,048 bytes。以 Admin `1c5531b` clean Git archive + Wing `dbdd70d4` 完成隔离 production build：2,872 modules；8 个 Primary 所属路由 chunk 均生成，无 `types/Pnw*` empty chunk，日志只剩 4 个 Host 通用依赖 empty chunk；当前源码闭包的真实 browser 矩阵仍待复验。
- 开发运行时曾把 Vue 与 Node 都挂到主 checkout `f399ac95`，导致用户看到旧 Header/边距而误判 HMR。Vue symlink 临时改挂 d11c 后用户确认 8D 恢复正常，七个当前 SFC URL 均为 200；但 Node 仍是主 checkout，缺少 `/maintenance/repair-plan`、`/maintenance/repair-ledger`，且旧已物化 manifest 仍把可见 test-runner 指向已删除的独立 View。该证据只证明前端 candidate 真源，不证明主 checkout 或真实 Host 闭包已修。
- 维护权限关卡 B 使用同一真实 `maintenance.vue` 与精确 Wing，另建内存 API fixture：`maintenance:read` 可显示 2 项 dry-run 且没有执行按钮/写计数，`maintenance:run` 必须经过确认弹窗后才产生 1 次模拟 POST；`test:read` 只显示固定清单且没有“全部运行”，无权限只显示 Cool 角色告警；`/test-runner` 深链仍进入同一维护 View。console warn/error 为 0；这只是 UI/capability 消费契约证据，不是整改 manifest 在真实 Host 的授权或数据库证据。
- standalone SQLite 清理继续按“先迁测试、后删恢复资产”推进：正式 async schema/migration runner 已拒绝非 PostgreSQL adapter，通用 runner/schema 测试已改用 PostgreSQL recording adapter；无调用者的通用 factory 与 SQLite adapter 公共导出已删除，login-policy 改为 4 个注入式契约测试，不再启动数据库。当前剩余 4 个 full-server legacy SQLite fixture 必须显式 opt-in，另有 1 个驱动专属 adapter 测试；只读审计由首个过渡快照 production 18 files / 63 matches 降至 13 / 54，current docs 保持 1 / 7 且 no-regression 通过；隔离全量 Vitest 为 30 files / 206 tests 通过、2 files / 9 个真实 PG 条件测试跳过，Server TypeScript `--noEmit` 与 production build 通过。真实旧库归档与 SQLite→PG 演练仍未取得，因此剩余 adapter、同步 schema 和唯一恢复工具未删除。
- Vue production build 的 9 个 Issue 路由入口及对应 CSS 共 18 个文件、186,774 bytes raw / 64,000 bytes gzip；用同一 Host commit `1a0d5ae` 的无插件临时 worktree 复算，完整 dist 增量为 38 个 raw 文件、293,515 bytes raw，以及 27 个 gzip 文件、101,339 bytes gzip。当前构建未产出 brotli；临时 worktree 已删除。

### 统一 Skill 完成度门禁

公共迁移 Skill `migrate-phoenix-admin-plugin` 已在 `phoenix-ai-workspaces` 形成可执行门禁，Function、BOM、Wing 和 Admin Node 任务也已收到规范路径。旧版本文档的 **96/100** 基于局部证据给分，不符合当前 Skill “强制门禁缺失时不得报告 90% 以上”的口径，现已撤回。在整改版本真实装配前不重算对外完成度：

| 维度 | 当前证据 | 结论 |
| --- | --- | --- |
| View/template/style 与 UI 差异 | 3 exact + 28 approved deltas；七个普通数据 View 的 MainBlock 10px 与 8 个 Wing PrimaryPanel/Section 已通过源码、契约和隔离 production build | 新 Primary 提交尚未装配；1440/720、明暗主题、Section 状态隔离与总 Primary 开关浏览器矩阵未通过 |
| MVC/TypeScript 算法与业务行为 | 14 个纯领域/算法文件；17 文件/101 项测试 | 源码门禁通过，真实数据深测待完成 |
| Host API、菜单、图标、dialog、命名空间 | endpoint/Controller 静态闭包；首次归属稳定“业务”组 | 整改版 200→404→200 与联合权限未运行，未通过 |
| 权限、数据、开发/生产 DDL、回滚 | 2 个 SQL 原始哈希、9 张 owned-table 三方闭包、dry-run/ledger 源码测试；Midway 资源角色 6 项服务矩阵和维护 UI 隔离权限矩阵通过 | 真实 Cool capability 联合矩阵、0002、字典物化、可信备份与恢复未运行，未通过 |
| 工程门禁与生产包 | manifest/settings-boundary/dictionary-contract/closure/UI/typecheck、17/101 tests、121 项 pack 且测试源码 0；当前布局隔离 production build 通过 | 插件级门禁通过；真实运行时同版本冷加载待复验 |
| 审计、文档、阶段提交与复用通知 | 证据文档与中文本地阶段提交持续形成 | 最终审计未发布，未通过 |

当前结论是 **迁移未收口，禁止生产切换**。只有上述强制门禁全部有当前版本的真实证据后，才重算 90%–100% 完成度；静态声明、旧运行时或 v1 恢复结果不能代替本轮整改验收。

## 3. 当前复用收益判定

| 能力 | 当前收益 | 证据等级 |
| --- | --- | --- |
| 登录与会话 | 不迁移 Login/OAuth/AppShell；actor 由 Host adapter 提供；legacy 登录/密码/第三方登录类型已从插件包删除 | 静态 Host-owned 边界闭环，当前运行时待复验 |
| 用户 | 复用 COOL 用户列表，只保留 Issue actor 最小投影；不建第二份平台用户，不修改 `base_sys_user` | `base:sys:user:list` 与只读 adapter 静态门禁通过，真实非 root 旅程待复验 |
| 路由与导航 | 技术 ID 与短产品 URL 解耦；页内导航由 Host Router 承担 | 已实测 |
| Wing Layout | 复用 Admin Wing 0.6 壳，只迁 View Contribution | 已实测 |
| 数据字典 | COOL 负责 namespaced 存储；重要度/紧急度和核心 listType 由插件 adapter 强制协议值，非法 Host 项不会进入页面 | 只读适配与测试已实测，管理写入待闭环 |
| Function | legacy View 原样迁入，插件拥有 `oip_function`，Issue 继续引用稳定 functionId；补充可审计的停用恢复闭环 | 用户已复核基准数据、新建编辑、搜索排序和 JSON 导出；开发者已用测试记录完成停用、停用筛选、确认启用和状态恢复 |
| 推送 / 8D | 插件拥有 `oip_push_record`、`oip_eight_d_report`；用户显示名复用 Cool，权限继续按列表成员判定 | 8D 新增/编辑/删除及 Issue 关联已用临时记录通过并清理；推送目标、共同成员预览和取消通过，接受/拒绝/撤回待点检 |
| 单元测试 | runner 仅执行固定插件脚本，不接收客户端命令/路径，生产默认不可用；独立 View 已并入维护页，旧地址只保留隐藏同 View 深链 | `73e181c5` 曾真实运行 101/101；本轮 Primary 改动后源码门禁仍为 17 文件/101 项，但测试 SHA 已变化，当前提交须重新做真实 Host 运行 |
| 部门、文件、审计、任务、备份 | repair/dictionary ledger 与 trusted-backup adapter 源码已完成；备份表范围三方闭合；部门/文件/Host task adapter 尚未闭环 | 备份真实生成/保留/恢复与其余 Host adapter 待运行或研究 |

最终审计必须把“已实测”和“计划复用”分开，不能提前把待办计入收益。
