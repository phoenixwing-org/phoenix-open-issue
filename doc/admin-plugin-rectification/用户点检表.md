# Open Issue Admin 插件用户点检表

状态：active

用途：每个迁移阶段完成后，由开发者先执行自动门禁，再由用户按本表确认真实页面、交互和产品语义。勾选结果是阶段验收记录，不用代码测试替代用户判断。

当前基线：`legacy/2cdc5ea`

当前插件：`phoenix-open-issue`

当前产品入口：<http://127.0.0.1:9000/open-issue/>

历史人工结论（2026-08-02）：`阶段有条件通过（96/100）`。该结论是合页前的基线证据，不直接代替当前 View 的人工验收。当前源码已将 Dashboard、8D、功能表收口为紧凑 Editor，并将单元测试并入带 Primary 的 `/open-issue/maintenance`；还需在精确 Host/Wing 运行时重物化后复验宽窄屏、明暗主题、新标签冷加载和权限组合。

## 1. 本轮开发挂载点检

- [x] 执行 `pnpm admin-plugin:status-dev-host`，Vue Host、Node Host 和 `driver.js` 均显示已挂载；
- [x] `phoenix-admin-vue` 的 `git status --short` 没有新增 `src/modules/phoenix-open-issue`；
- [x] `phoenix-admin-node` 的 `git status --short` 没有新增 `src/modules/phoenix-open-issue`；
- [x] 两仓执行 `git check-ignore -v src/modules/phoenix-open-issue`，来源是本机 `.git/info/exclude`；
- [x] Host 的 tracked `.gitignore`、配置、文档和提交中没有 Open Issue 产品路径；
- [x] 重启 Vue/Node 后，9000 和 8101 均可访问。

## 2. 插件生命周期点检

- [x] 用 Host 根管理员进入 `/pah/plugins`；
- [x] 粘贴 `packages/admin-plugin/manifest.json` 后能够“校验并登记”；
- [x] 插件名称为 `Open Issue`，技术 ID 为 `phoenix-open-issue`；
- [x] 依次执行“安装”“启用”，状态最终为“已启用”；
- [x] 刷新权限菜单后出现 `业务 / 问题工作台`；如果 Host 管理员另建 `ISSUE` 大分组，可再把模块分配过去；
- [ ] 停用时 ISSUE 菜单消失，但插件源码和业务数据不被删除；
- [ ] 再次启用后菜单恢复；已有角色授权恢复能力在后端单元测试中通过。

## 3. 页面保真点检

对照 legacy 页面检查信息结构、按钮位置、表格密度、弹窗字段和交互顺序，不以“功能能打开”代替保真验收。

- [x] `/open-issue/` 自动进入 `/open-issue/lists`；
- [x] `/open-issue/dashboard`：仪表盘和任务中心布局与 legacy 初步接近；
- [x] `/open-issue/lists`：列表管理、筛选、列设置和列表单元格结构初步接近；
- [x] `/open-issue/list/:id`：列表详情、成员管理、Issue 表格和快速编辑入口一致；
- [ ] `/open-issue/issue/:id`：基本信息、点检内容和 8D 入口一致；时间线排序/复制/新增图标已修复，等待用户复看；
- [x] `/open-issue/push-history`：推送记录页面结构初步接近；
- [x] `/open-issue/reports/8d`：8D 列表页面结构初步接近；
- [ ] `/open-issue/functions`：功能简表、筛选、排序、新建/编辑/停用和导入导出与 legacy 一致；停用/恢复已由开发者复验，XLSX 导入仍待用户点检；
- [ ] `/open-issue/maintenance`：Primary 可切换“数据修正 / 单元测试 / 修正审计”，测试文件、运行按钮、最近结果和环境限制仍完整；
- [ ] `/open-issue/test-runner` 仅作隐藏兼容深链，打开同一个维护 View 并默认进入单元测试区；
- [x] 帮助按钮及引导内容可打开，页面不会因为 `driver.js` 缺失报错；
- [ ] 切换 Dashboard、8D、Function 和维护三个区段并刷新后，页面仍可进入且无空白区域；
- [x] 用户完成本轮操作后，浏览器控制台没有 Vue/Router/Pinia 运行时错误；`cl-svg` Host 告警单独记录。

## 4. 当前阶段允许失败的项目

以下项目已完成接口迁移但仍需真实业务数据深测；不能通过改写 UI 绕过：

- [x] 真实 Issue 列表、详情和新增；
- [x] 点检新增和时间线；编辑、状态切换继续深测；
- [ ] 推送发起、接受、拒绝和撤回；接口与空历史页面已通过；
- [x] 8D 报告新增、编辑、删除及 Issue 关联；使用临时记录回归后已清理，列表恢复为空；
- [ ] Issue 领域权限与 Host capability 的联合判定；
- [ ] 数据字典受保护写入、文件、审计、任务和备份的真实 Host 复用；
- [ ] Open Issue PostgreSQL 数据修正/升级维护页；菜单和页面已由开发者复验，未执行修正任务，等待用户确认页面语义。
- [ ] 正式发布包在 `synchronize: false` 下接入可信备份验证器，执行 Pah dry-run/受控安装并留下真实 migration ledger；插件 SQL 已完成空库执行与隔离恢复演练，维护页的数据修正不代替建表迁移。

## 5. 自动门禁记录

开发者交付前必须填写；用户通常只需要复核结果与失败原因。

| 门禁 | 期望 | 本轮结果 |
| --- | --- | --- |
| `pnpm admin-plugin:verify` | manifest、Host-owned settings boundary、dictionary contract、UI 差异、类型和插件测试通过 | 2026-08-03 通过；3 exact + 28 approved deltas，17 文件/101 项，pack 121 项/145,048 bytes |
| manifest v2 / SQL 制品 | descriptor、声明与 SQL 一一对应，SHA-256、路径和大小通过 | 2026-08-03 通过；2 个 SQL，共 7,782 bytes |
| PostgreSQL DDL / 恢复 | 事务空库执行；隔离备份恢复表、索引、演示数据与黄金查询一致 | 2026-08-02 通过；8 表、43 索引、8 条记录，临时库已删除 |
| Node production 装配 | 隔离克隆生成实体、编译并把 descriptor/SQL 原样复制进 dist | 2026-08-02 通过；dist SQL 6,943 bytes，SHA 与 manifest 一致，临时克隆已删除 |
| Pah v2 真实 dry-run | 只读校验制品和计划，不显示 DDL 普通安装/执行入口 | 2026-08-02 通过；1 个待应用 SQL、事务必须、备份需可信证明 |
| Vue `pnpm run type-check` | 受控插件挂载后通过 | 2026-08-02 通过 |
| Vue `pnpm run build` | 真实打入 9 个 Issue View 并记录增量体积 | 2026-08-02 通过；路由入口 186,774/64,000 bytes，完整 dist 增量 293,515/101,339 bytes（raw/gzip） |
| Admin Host 路由 | 冷 hidden 参数深链正确归属，真实 404 不循环 | 2026-08-02 浏览器通过；Host commit `1a0d5ae` |
| Node `pnpm run verify` | lint、16/16 Pah 测试、实体生成和构建通过 | 2026-08-01 通过 |
| Host Git 隔离 | 挂载点只命中 `.git/info/exclude` | 2026-08-01 通过 |
| 浏览器点检 | 插件启用、路由可达、真实 CRUD、无运行时错误 | 列表/Issue/点检、Function、测试和 8D CRUD 通过；推送预览通过；维护菜单与页面可用 |

## 6. 2026-08-01 用户初检记录

| 点检项 | 结果 | 说明 |
| --- | --- | --- |
| `/pah/plugins` 中显示 Open Issue 已启用 | OK | manifest 已重新登记；显示导航模块 2、数据表 7 |
| `业务 / 问题工作台` | OK | Ribbon 入口已出现 |
| `/open-issue/` 根路径重定向 | OK | 自动进入 `/open-issue/lists` |
| 仪表盘、列表、推送历史、8D 等 UI | 初步 OK | 整体接近 legacy；因无业务数据，详情和深层交互尚不能完整确认 |
| Issue Ribbon 图标 | OK | `pnw:dashboard/list/history/report` 均为真实 SVG 且无 fallback；旧问号来自旧标签模块缓存 |
| 新建及数据操作 | 部分 OK | 已真实创建列表、Issue 和点检并显示 Host 用户名；推送和 8D 尚未迁移 |
| 页面帮助 | OK | 引导正常显示 |
| Console | 已知待办 | 存在业务接口报错，当前阶段允许；后端迁移后必须清零 |

### 2026-08-02 用户复核

| 点检项 | 结果 | 说明 |
| --- | --- | --- |
| Function 基准数据 | OK | 可见 `Phoenix / MIG-001 / Issue 插件迁移点检` |
| Function 新建/编辑 | OK | 字段顺序、弹窗布局和表格密度与 legacy 一致 |
| 仪表盘、问题列表、推送历史、8D Ribbon 图标 | OK | 无问号；普通状态灰色，选中状态蓝色 |

## 7. Function 与合并维护 View 用户复核

开发者预验收已经通过，以下勾选必须由用户在真实页面完成：

- [ ] Ribbon `业务 / Open Issue 管理` 下只显示“功能”和“维护”，不再出现独立“单元测试”菜单；
- [x] 打开 `/open-issue/functions`，能看到 `Phoenix / MIG-001 / Issue 插件迁移点检`；
- [x] Function 新建/编辑弹窗的字段、顺序和表格密度与 legacy `/functions` 一致；
- [ ] 搜索、平台筛选、外部 ID 数字排序和 JSON 导出已由用户通过；开发者已用测试记录完成停用→停用筛选→确认启用→恢复启用，XLSX 导入仍待用户复核；
- [ ] 打开 `/open-issue/maintenance`，在 Primary 中切换到“单元测试”，显示当前固定白名单文件和用例总数；
- [ ] 仅有 `maintenance:read` 时能点击“预览 dry-run”并看到变化数量，但看不到执行按钮且数据库零写入；增加 `maintenance:run` 后才出现确认执行；
- [ ] 点击“全部运行”后显示当前通过结果，且页面没有接受任意命令或路径的输入框；
- [ ] 在仪表盘、Function、维护区段之间切换并刷新，不出现注入错误、空白或 404。

历史开发者证据（2026-08-02）：独立测试 View 曾在右侧内置浏览器运行 84/84，且无 `route location` / `getActivePinia` 注入错误。当前合并后源码门禁为 17 文件/101 项，共享最新 Wing 页面布局及 8 个 PrimaryPanel/Section 的隔离 production build 已通过。2026-08-03 又以真实合并 View + Wing `0.6.2` 的隔离 fixture 验证 `/maintenance` 和 `/test-runner` 是同一“Open Issue 维护”页面、共用同一 Primary，后者默认进入单元测试；宽窄屏/明暗主题无标题重叠和 console warn/error。后续权限夹具还验证：只读维护角色仅能预览 dry-run 且零写计数，执行角色必须二次确认才产生 1 次模拟 POST，测试只读角色无运行按钮，无权限角色只显示告警。Primary 源码和受控测试树本轮已经变化，因此当前提交仍须重新完成 101/101、真实菜单、Section 折叠状态隔离及数据库零写点检。

## 8. 问题记录

| 日期 | 页面/操作 | 实际现象 | legacy 期望 | 严重度 | 处理状态 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-01 | `/pah/plugins` | 初次未显示 Open Issue | 已登记插件应在治理页可见并显示真实状态 | 阻断 | 已修复，当前已启用 |
| 2026-08-01 | Issue Ribbon | 初次为空/短暂问号 | 保留 legacy 对应图标 | UI 偏差 | 已修复；硬刷新后真实 SVG、无 fallback |
| 2026-08-01 | 列表/Issue/点检 | 初次无数据且无法新建 | 支持完整 CRUD 和详情点检 | 接口待迁移 | 首个真实纵切已通过 |
| 2026-08-01 | Function/单元测试 | manifest 已登记但已启用实例仍只有旧 6 条菜单，新增路由 404 | 新 View 在插件 contribution 重物化后可达 | 联调 | 已通过停用→启用重物化；两页开发预验收通过 |
| 2026-08-01 | 数据库修正 | Admin 插件中看不到旧设置页签 | `oip_` 数据检查和幂等修正属于插件维护能力 | 功能遗漏 | 归属已修正，待迁移 PostgreSQL repair View/Service |
| 2026-08-02 | `/open-issue/test-runner` | `useRoute` 与 Pinia 注入缺失，访问后 View 空白 | 外部插件必须复用 Host 的同一 Vue/Router/Pinia 浏览器模块实例 | 阻断 | 已修复；Host 与插件统一使用 Vite 预构建单例，右侧浏览器运行 84/84 通过 |
| 2026-08-02 | Issue 时间线按钮 | 排序、复制、添加只显示圆形按钮，图标为空 | 显示 legacy 的 Sort、DocumentCopy、Plus 图标 | UI 偏差 | 已修复；插件组件显式导入图标，不依赖 legacy `main.ts` 全局注册 |
| 2026-08-02 | Function 停用 | 点击后无确认框；legacy 也没有停用项恢复入口 | Host 统一显示 Wing 选择框，插件能查看停用项并重新启用 | 阻断 | 已修复；Host 根层唯一 `PnwChoiceDialogHost`，测试记录完成停用、停用筛选、确认启用并恢复原状态 |
| 2026-08-02 | 推送历史 / 8D | 页面请求接口 Not Found 或无后端闭包 | 原页面使用真实 Postgres 插件数据 | 接口待迁移 | 已迁移；8D CRUD/Issue 关联通过并清理，推送目标与共同成员预览通过，处理生命周期待点检 |
| 2026-08-02 | 生产 DDL | 旧 manifest 无 SQL 制品、无可执行恢复路径 | 生产关闭 synchronize，迁移可审计且可恢复 | 发布阻断 | manifest v2、SQL/checksum、Pah dry-run/ledger/事务契约和隔离恢复演练已完成；可信备份验证器仍是正式切换门禁 |
| 2026-08-02 | 数据库修正 | legacy 设置页混合 Host 和 Issue 能力 | 只保留 Issue 插件自有维护，不复制平台设置 | 功能遗漏 | 已新增并重物化 `/open-issue/maintenance`；任务、边界提示和结果区通过开发者浏览器点检，未执行修正 |

严重度建议：`阻断`、`UI 偏差`、`接口待迁移`、`体验建议`。

## 9. 本地卸载与恢复

完成本轮开发或需要证明 Host 可脱离插件运行时：

```bash
pnpm admin-plugin:unmount-dev-host
```

- [ ] 只删除两条插件源码软链接和插件创建的依赖软链接；
- [ ] 两仓 `.git/info/exclude` 中的本插件标记被移除；
- [ ] Vue/Node Host 重启后仍能独立运行；
- [ ] `/pah/plugins` 中先停用或卸载本地登记，避免留下不可加载菜单；
- [ ] 需要恢复时重新执行 mount、重启 Host、启用插件即可。
