# 旧站迁移脱敏 fixture

`migration-v1-full.sanitized.json` 是从 2026-08-03 旧站完整导出生成的结构等价脱敏样本，用于本地只读预检、服务端迁移计划与回归测试。

- 保留 `version=1`、`exportScope=full`、8 类业务表 41 行以及表间引用关系；原包的列表组织引用也保留在 fixture 中，专门验证迁移提交会把它们统一置空。
- 保留 4 个用户、5 个组织和 52 个字典项的结构与计数，用于验证它们被业务提交物明确排除。
- 所有实体 ID、账号、邮箱、组织名、业务文本、功能标识和字典显示名均为测试值；密码哈希为不可登录的显式占位符。
- 不得用此 fixture 作为生产数据恢复源；实际执行仍需 Host 显式映射、可信 PostgreSQL 备份、恢复演练和服务端单事务执行器。

固定 SHA-256：`4ce585cc6bcf0bf6b2d4057bd6b91d7436ddf7e0689203f40d54a3746099a130`。
