# 请按数据库类型复制一个完整模板为 .env：
#
# SQLite：
#   cp packages/server/.env.sqlite.example packages/server/.env
#
# PostgreSQL：
#   cp packages/server/.env.postgres.example packages/server/.env
#
# 两种数据库不能同时配置。服务不会在 PostgreSQL 失败时回退到 SQLite。
# 两个数据库模板都包含默认关闭的飞书登录配置；真实 App Secret 不要提交到仓库。
