# 正式 standalone 只使用 PostgreSQL：
#   cp packages/server/.env.postgres.example packages/server/.env
#
# 缺 DATABASE_URL 时服务拒绝启动，不会创建本地数据库或回退旧驱动。
# legacy 恢复模板仅供已登记的旧库→PostgreSQL 导入演练，不是开发/生产默认入口。
# 真实密码、连接串和 App Secret 不要提交到仓库。
