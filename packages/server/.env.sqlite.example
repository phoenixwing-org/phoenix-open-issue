# SQLite 后端配置

PORT=3400
JWT_SECRET=change-me-in-production

# 相对 packages/server 目录；生产环境建议使用绝对路径。
DB_DRIVER=sqlite
DB_PATH=../../data/open-issue.sqlite

# build 后自动托管 packages/web/dist；前后端分离时设为 false。
# SERVE_STATIC=true
# STATIC_DIR=../web/dist
